# Some resource files that came with the React-App install are omitted because I'm lazy
## You can get them by installing a React-App with `npm create react-app [name]` or your console's version (just google it)
